# utakaha.com
Static Site Generator for utakaha.com

## Setup
```
bundle install
```

## Usage
Build the static files into `public/`:
```
bin/build
```

Build the static files, serve the site at http://localhost:8000, and rebuild when source files change:
```
bin/server
```
